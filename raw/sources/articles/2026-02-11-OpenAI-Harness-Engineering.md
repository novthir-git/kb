# Harness engineering: leveraging Codex in an agent-first world（中文结构化研究摘录）

- 原文：https://openai.com/index/harness-engineering/
- 作者：Ryan Lopopolo（OpenAI）
- 发布日期：2026-02-11
- 检索与入库日期：2026-09-23
- 检索方式：WebFetch 与直连均返回 403，经内置浏览器读取官方中文版（标题「工程技术：在智能体优先的世界中利用 Codex」，
  "harness engineering" 这一术语在译文中丢失）
- 存档说明：要点式中文摘录，非翻译或镜像；保留数字、口径与作者自述的边界，措辞与细节请回查原文。
- 证据性质：公司自述的内部实验，无外部审计、无对照组。

## 设定与规模

- 2025-08 下旬从空仓库起步，五个月内不人工编写任何代码（刻意施加的约束）；产品为有内部用户与外部 alpha 测试者的内部 beta。
- 约 100 万行代码、约 1,500 个 PR；团队 3 人起步、现为 7 人，人均每天约 3.5 个 PR，扩员后人均吞吐反而上升；
  作者自估用时约为手写的 1/10。
- 单次 Codex 运行常在单个任务上持续 6 小时以上。

## 要点

1. **瓶颈在环境而非模型**：早期进展慢，原因是缺工具、抽象与内部结构。卡住时追问"缺什么能力、怎样让它对 agent
   可读且可强制执行"。人主要通过提示工作，审查大部分转为 agent 对 agent。
2. **应用对 agent 可读**：吞吐上来后瓶颈变成人工 QA。每个 worktree 起独立应用实例与临时可观测性栈（LogQL / PromQL），
   并接入 Chrome DevTools 协议，使性能与用户旅程约束可由 agent 自行验证。
3. **AGENTS.md 是地图而非手册**：大一统 AGENTS.md 失败，作者列出四条原因——挤占上下文；一切都重要等于都不重要；
   迅速腐烂成陈旧规则；单个 blob 无法机械核查覆盖率、新鲜度、所有权与交叉链接。改为约 100 行的目录，指向结构化
   `docs/`（带验证状态的设计文档、产品规格、执行计划与技术债追踪、生成物、面向 LLM 的参考资料）；由 linter 与 CI
   校验新鲜度与交叉链接，并有定期运行的 doc-gardening agent 发修复 PR。立场：agent 运行时访问不到的知识，对它等于不存在。
4. **依赖取舍**：偏好能在仓库内被完整推理的"枯燥"技术。有时自写功能子集比绕开不透明的上游行为更便宜
   （例：自写带并发的 map 辅助函数，取代通用并发库）。
5. **强制不变量而非管实现**：每个业务域固定分层 Types → Config → Repo → Service → Runtime → UI，横切关注点只能经
   Providers 进入，其余依赖边一律禁止，由自定义 linter 与结构测试执行；另有少量"品味不变量"（结构化日志、命名、
   文件大小上限等），lint 错误信息内嵌修复指令。作者判断：人类团队常推迟到数百名工程师规模才引入的架构，
   对 agent 是早期前提。
6. **合并理念**：高吞吐下纠错便宜、等待昂贵，故减少阻塞式合并门，偶发失败靠重跑；作者承认这在低吞吐环境中不负责任。
7. **熵与垃圾回收**：agent 会复制仓库中已有的坏模式。起初每周五（一周的 20%）人工清理"AI 残渣"，不可扩展；
   改为把"黄金原则"编码为机械规则，并定期运行后台任务扫描偏差、更新质量等级、发定向重构 PR，多数一分钟内审完并
   自动合并。作者的类比：技术债是高息贷款，宜持续小额偿还。

## 作者自述的边界

- 端到端驱动新功能的能力高度依赖该仓库的结构与工具，不应在没有类似投入时假定可泛化。
- 全 agent 生成系统的架构连贯性如何随时间演化、人的判断在哪里最有价值，尚不清楚。
- 结论：纪律从代码转移到支撑结构；最难的是设计环境、反馈回路与控制系统。
