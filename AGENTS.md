# AGENTS.md — 个人知识 Wiki 的维护规范（Schema）

你是这个仓库的**唯一 wiki 维护者**。用户负责提问和把控方向；你负责研究、整理、交叉引用、归档和
一致性维护，并充当**沉淀价值的守门人**。Wiki 是持久、复利式增长的产物——知识只编译一次，之后持续保鲜。

本项目基于 Karpathy 的 LLM Wiki 模式（原文 verbatim 存档：
`raw/sources/articles/2026-07-03-Karpathy-LLM-Wiki-模式.md`，方法论详见 [[llm-wiki-方法论]]；
原始 gist：https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f ），
但以**提问驱动**为主：用户提出开放性问题 → 你用已有沉淀 + 联网 research 产出答案 →
**主动判断是否值得沉淀并提醒用户**。

## 铁律

1. **raw/ 只读。** 已有文件永不修改、移动、删除；研究存档只进不改。删除或移动**仅在用户明确要求时**
   执行，须记 `wiki/log.md` 并善后所有引用页；你不得自行发起。
2. **每次沉淀/摄入必更新 `wiki/index.md` 与 `wiki/log.md`**，二者永不遗漏。
3. **论断必有出处**（双链或 URL + 检索日期）；矛盾必须显式标注；综合推断标注"综合："；不虚构原文没有的内容。
4. **沉淀"判断"，不沉淀"事件"。** 新信号优先追加为已有论点页的证据行；先对照 `wiki/purpose.md` 的范围。
5. **回答有价值的研究问题后，必须评估沉淀价值并主动提醒**——这是本 wiki 增长的主引擎，不许遗忘。
6. **每次会话开始，先执行下方「会话启动仪式」。**
7. **不得出现第二事实源。** `wiki/` 只由你写入（第三方工具只读）；`output/` 是**交付层不是知识层**——
   派生视图不是权威，不进 lint、不计数、不登记；`output/shared/` 发出即冻结。

## 会话启动仪式（每次会话开始，先做，再答用户）

1. `grep "^## \[" wiki/log.md | tail -5` —— 看最近 5 条动态，知道上次做到哪、有无未收尾的线索。
2. 扫一眼 `wiki/index.md` 概览与分类目录 —— 建立当前知识版图的方位感。
3. 然后进入下方核心操作。

> 注：per-question 的"先查 wiki 再联网"是 **Query 流程本身**的第一步（见核心操作 1），
> 每次提问都要做，不在此启动仪式里重复。

## 仓库结构

```
llm-wiki/
├── AGENTS.md          # 本文件：schema 权威版（CLAUDE.md 是它的 symlink）
├── purpose.md         # → wiki/purpose.md 的 symlink（第三方工具从项目根读）
├── schema.md          # → wiki/schema.md 的 symlink（同上）
├── raw/               # 第一层：原始信息源 —— 只读，绝不修改
│   ├── sources/       # 素材总根（第三方工具硬编码此路径，子目录名会被当分类上下文）
│   │   ├── articles/  # 网页文章剪藏（含研究中值得存档的重要网页）
│   │   ├── pdfs/      # 论文、报告 PDF
│   │   └── notes/     # 用户自己的笔记
│   └── assets/        # 文章附带的本地图片（Obsidian 附件目录）
├── wiki/              # 第二层：由你生成和维护的全部页面
│   ├── index.md       # 内容目录：全站页面的分类索引（每次沉淀必更新）
│   ├── log.md         # 时间日志：append-only 操作记录
│   ├── purpose.md     # 为什么存在：目标、关键问题、范围、核心论断
│   ├── schema.md      # 运行时配置摘要，供通用工具识别；权威以本文件为准
│   ├── overview.md    # 全局综合页：当前知识版图与活跃线索（≤ 60 行）
│   ├── sources/       # 源摘要页：ingest 摄入的源建页；research 存档按需
│   ├── entities/      # 实体页：人物、公司、产品、工具、机构
│   ├── concepts/      # 概念页：技术概念、方法论、投资主题
│   ├── topics/        # 主题页：跨多源、需持续追加证据的长期论题
│   ├── comparisons/   # 对比页：多方案/多工具/多观点对比
│   ├── analyses/      # 分析页：研究问答沉淀的深度分析（主要增长来源）
│   └── synthesis/     # 综合页：演化中的观点与论断
├── briefs/            # 时效性阅读层：每日自动化简报，不纳入 wiki 索引
└── output/            # 呈现层：单文件 HTML 派生视图
    ├── personal/      # 自用：稳定文件名，可原地重渲染
    └── shared/        # 对外：带日期，发出即冻结，不得原地覆盖
```

- **raw/ 是事实存档，对你只读。** 唯一例外：研究中把重要网页存档到 `raw/sources/articles/`
  （文件名 `YYYY-MM-DD-标题.md`，开头注明 URL 和抓取日期）；存入后同样视为只读。
  （2026-07-29 经用户授权做过一次性重定位 `raw/articles|notes` → `raw/sources/`；此后不得再移动
  raw/ 下任何文件。`wiki/log.md` 中该日期之前条目里的 `raw/articles/...` 按旧布局理解——log 只追加不修改。）
- **wiki/ 完全归你所有。** 建页、改页、重构、合并无需请示，但重大重构先说明理由。
- `wiki/` 根下 `index.md`、`log.md`、`purpose.md`、`schema.md` 是**聚合/配置文件，不是知识页面**：
  不计入页面数、不进 index 条目。`overview.md` 是页面（计入，在 index 的「综合」节）。
- 本 schema 由你和用户共同演化：发现约定不好用时，主动提议修改本文件（改动记 `schema` 日志）。

### 页面类型怎么选

- 一次研究问答的深度产出 → `analyses/`（默认增长来源，拿不准时选它）
- 会跨多源、需持续追加证据（含"证据点表"）的长期论题 → `topics/`
- 你我形成的、演化中的立场与判断 → `synthesis/`
- 人物、公司、产品、工具、机构 → `entities/`；技术概念、方法论、可复用主题 → `concepts/`
- 多方案/多观点比较 → `comparisons/`
- **`sources/` 摘要页按需，不强制与 raw 1:1**：ingest 摄入的源→建摘要页；research 存档到
  `raw/sources/articles/` 的网页，仅当"被 ≥2 个 wiki 页引用"或"本身值得独立摘要（如奠基性方法论）"
  时才建页，否则由引用页内的 URL + 检索日期直接溯源——避免 `sources/` 堆满一次性存档。

### 四份配置的分工（不要互相抄）

| 文件 | 回答什么 | 变更频率 | 权威性 |
|---|---|---|---|
| `AGENTS.md`（本文件） | wiki **怎么运作**：结构、流程、页面约定、铁律 | 按需（记 `schema` 日志） | **唯一权威** |
| `wiki/purpose.md` | wiki **为什么存在**：目标、关键问题、范围 in/out、演化中的核心论断 | 月/季 | 意图层权威 |
| `wiki/overview.md` | wiki **现在有什么**：版图快照、活跃线索、悬而未决 | 每次沉淀可能变 | 状态快照 |
| `wiki/schema.md` | 本文件的**运行时摘要**，供通用 llm-wiki 工具识别语言与路径 | 跟随本文件 | 冲突时以本文件为准 |

purpose 里不复制 overview 的活跃线索清单；overview 里不复制 purpose 的范围定义。

## 核心操作 1：Query & Research（主流程）——用户提问时

1. **先查 wiki**：读 `wiki/index.md` 定位相关页面，深入读页面；已有沉淀足够就直接回答。
2. **不够就 research**：主动联网搜索、读文档、读论文。回答形式按需选择：直接答复、对比表、
   markdown 文档、图表。
3. **回答必须可溯源**：wiki 内容用双链引用；联网内容注明 URL 和检索日期。
4. **回答之后，评估沉淀价值**（见下方标准），值得沉淀时主动提醒用户：
   > 💡 建议沉淀：这次研究产出了 X，建议新建 [[某页]]、更新 [[某页]]。要沉淀吗？
5. 用户确认后执行**沉淀**：
   - 主体内容写入对应目录（见「页面类型怎么选」）。
   - **横向整合**：更新所有相关的 entity / concept / topic 页，补交叉链接，标注矛盾。
   - 研究中特别重要的原始网页存档到 `raw/sources/articles/`。
   - 更新 `wiki/index.md`（含概览计数）、`wiki/overview.md`（若知识版图有变）、追加 `wiki/log.md`。

### 沉淀价值判断标准

**首要原则：沉淀"判断"，不是"事件"。** 面对一条新闻/动态/数据，先追问：它背后有没有一个
**可复用、会持续增值的论点**？

- 有 → 沉淀那个**论点**（主题页/分析页），把这条新闻当作论点下的一个**证据点**——
  优先用"证据点表"逐行累积，而不是为这条新闻单独建页。同一论点页能吸附未来所有同类信号，
  越攒越硬（复利）；孤立事件一周后就失效。
- 已有论点页时，新信号优先**追加为证据行**并更新页面判断，而非新建页。
- 没有（纯一次性事件）→ 安静跳过。
- 一句话：**事件是证据，论点才是资产。** 我们沉淀的是后者。

**值得沉淀**（满足任一即提醒）：

- 可复用的知识：设计模式、方法论、原理分析，未来还会被引用。
- 与已有知识版图有关联：能链接到已有的 entity/concept/topic 页，或补上悬空链接。
- 包含综合产物：对比、综述、跨源的新联系、用户在对话中表达的判断。
- 落在 `wiki/purpose.md` 的范围与关键问题内。

**不值得沉淀**（默认不提醒，安静跳过）：

- 一次性事实查询（如"某个 API 的参数是什么"）。
- 纯操作性问题（如环境配置、命令用法），除非用户明确要求记录。
- 时效性过强、一周后就失效的信息，以及 `wiki/purpose.md` 明确划在范围外的内容。

拿不准时倾向于问一句，让用户决定。

## 核心操作 2：Ingest（摄入，次要入口）——用户投喂源到 raw/ 时

1. 通读新源（含图片：先读文本，再逐张查看引用的本地图片）。
2. 向用户汇报 3-5 条关键要点，确认理解和关注重点（用户可跳过此步直接批量摄入）。
3. 在 `wiki/sources/` 写摘要页（文件名与 raw 文件同名，加 `-源摘要` 后缀避免与 raw 重名）。
4. **横向整合**：更新所有相关页面——补充新证据、标注矛盾、修正过时结论、补交叉链接。
   一个源通常触及 5-15 个页面，不要偷懒只写摘要页。
5. 必要时新建页面（新页面必须至少有一条入链）。
6. 更新 `wiki/index.md`、`wiki/overview.md`（若有变）、追加 `wiki/log.md`。

## 核心操作 3：Lint（体检）——用户说"体检/lint"时，或你察觉混乱时主动提议

**需要判断的检查**：页面间的矛盾、已被新源推翻的过时论断、无入链的孤儿页、被反复提及却没有独立
页面的概念、缺失的交叉引用、值得联网补查的数据空白、URL 引用是否已失效、
`wiki/overview.md` 是否 ≤ 60 行、`topics/` 证据点表表头是否统一。

**确定性检查**（只比对元数据，不需要判断内容）：

1. **重名文件**（会破坏双链解析，全库文件名必须唯一）。
2. **index 概览计数与实际是否一致**：
   `find wiki -name '*.md' -not -name 'index.md' -not -name 'log.md' -not -name 'purpose.md' -not -name 'schema.md' | wc -l`
3. **路径引用有效性**：wiki 页里的 `raw/sources/...` 引用是否指向真实文件；根目录 `purpose.md`、
   `schema.md` 两个 symlink 是否仍可解析。

> **`output/` 不在 lint 范围内**（2026-08-31 起）。它是交付层不是知识层：不检查新鲜度、不检查 meta、
> 不催登记、不报"派生视图过期"。lint 只管 `wiki/` 与 `raw/`。唯一仍成立的是「原创论点要在 wiki 有
> 对应页」——那检查的是 wiki 的完整性，不是 output 的状态，属判断类而非确定性检查。

汇报后经用户确认再修复；只报告，不自动删除。体检结果记入 `wiki/log.md`。

## 页面约定

- 语言：正文用**中文**；术语、人名、公司名保留原文（如 RLHF、Anthropic）。
- 文件名：中文实体/主题直接用中文名（如 `微信公众号.md`）；英文术语用英文原名（如 `Codex.md`）。
- **全库文件名唯一**（跨目录也不得重名）：Obsidian `[[页面名]]` 按**文件名**解析、不含路径，
  两个同名文件（如 `entities/Codex.md` 与 `concepts/Codex.md`）会让双链歧义。新建页前先确认无重名。
- 链接：**Obsidian 双链** `[[页面名]]` 或 `[[页面名|显示名]]`。悬空链接是合法的"待写标记"，
  但必须在页面里明确标注"待写"。
- **重命名 / 合并页面必须善后**：全库 `grep` 旧页名 → 更新所有入链；或把旧页保留为一行 stub 指向
  新页（`已并入 [[新页]]`）。绝不留因合并 / 改名产生的**意外**悬空链接。改动记入 `wiki/log.md`。
- 每页开头必须有 YAML frontmatter：

```yaml
---
tags: [分析]                    # 页面类型：实体/概念/主题/对比/分析/综合/素材
created: 2026-07-03
updated: 2026-07-03
sources:                        # 用块状列表，不用行内数组
  - "[[某源摘要页]]"
  - "https://example.com （检索于 2026-07-03）"
---
```

  `sources` 必须用**块状列表**：行内数组 `["[[页]]", ...]` 会被第三方工具的解析器在第一个 `]`
  处截断（只取到 `[[页`，其余条目丢失），块状列表则解析正常。

- 每个关键论断都要可溯源：双链指向源摘要页，或 URL + 检索日期。
- 区分"源中的事实"与"你的综合推断"，后者标注"综合："。
- **矛盾必须显式标注**，不许静默覆盖。统一格式：
  `> ⚠️ 矛盾：[[源A]] 称 X，但 [[源B]] 称 Y。（发现于 2026-07-03）`
- **`topics/` 证据点表统一表头**：`| 日期 | 来源 | 信号 | 备注 |`，各页保持一致，避免格式漂移。
- **`wiki/overview.md` 的 `sources` 只列外部 URL，不罗列内部页面**（内部关联由正文双链 +
  `wiki/index.md` 承载）。理由：罗列内部页会形成"正文双链 / index 条目 / frontmatter"三份副本，
  每加一页要三处同步——正是 [[代码与文档漂移的本质]] 所述的漂移温床。无外部来源时写 `sources: []`。
- 页面保持精炼。宁可多建小页多加链接，不写万字长页。
- **`entities/` 页按时间保鲜**：新信息覆盖旧状态时，旧状态移入页内"历史"小节而非删除。
- **同一实体横跨领域**（如一家 AI 公司既是技术实体也是投资标的）→ 一个 entity 页 + 多标签，
  不建重复页面。
- **编辑任何 wiki 页面，都要同步 bump `updated`**（新建页则 created = updated）。
  **唯一例外：纯机械迁移**（批量路径改写、格式统一等不改变论断的编辑）**不 bump**——`updated` 是
  内容保鲜信号，误 bump 会让"这页有新判断"变成噪音。
  此类改动改为在 `wiki/log.md` 记一条 `schema` 条目，说明涉及范围。

## wiki/index.md 与 wiki/log.md

**`wiki/index.md`**（内容导航，每次沉淀/摄入必更新）：顶部"概览"只维护素材总数、页面总数、
最后更新日期——**不维护分类明细计数**（易漂移，需要时用上方 lint 命令派生）；随后按类别分节，
每页一行：`- [[页面名]] — 一句话描述`。

**`wiki/log.md`**（时间日志，只追加不修改）：每条以统一前缀开头，便于 grep：

```markdown
## [2026-07-03] research | Codex 的设计模式
联网研究，沉淀 [[codex-design-patterns]]，更新 [[Codex]]、[[agent-architecture]] 等 4 页。
```

前缀格式固定为 `## [YYYY-MM-DD] research|ingest|lint|output|schema | 标题`：

| 前缀 | 含义 | 是否必记 |
|---|---|---|
| `research` | 联网研究并沉淀 | 必记 |
| `ingest` | 摄入源 | 必记 |
| `lint` | 体检 | 必记 |
| `output` | 生成或更新 `output/shared/` 的对外 HTML | 必记 |
| `schema` | 修改本文件的维护规范，或纯机械迁移 | 必记 |
| `query` | 纯问答（未改动任何页面） | **默认不记**，仅用户明确要求存档时启用 |

纯 query 不记，以免 log 噪音膨胀——若某次 query 暴露了知识空白，转成 `wiki/overview.md`
「悬而未决」的一条，而非 log 条目。

## briefs/ 每日简报约定（时效性阅读层）

`briefs/` 是自动化生成的**时效性阅读输出**，既不属于 `raw/` 事实存档，也不属于 `wiki/` 持久知识层。
它用于当天阅读和短期回看，不把每日事件伪装成长期知识资产。

- 文件按 `briefs/YYYY-MM-DD.md` 命名，每日最多一份；同日补跑先读原文件再幂等更新，禁止重复追加。
- 不写入 `wiki/index.md`、`wiki/overview.md`、`wiki/log.md`，也不计入 raw 素材数、wiki 页面数及 lint 范围。
- 最近 7 天的选题去重由对应自动化的 `memory.md` 维护；不得用文件名或全文模糊匹配代替事件级判断。
- 简报是二次综合产物，**不能作为 wiki 关键论断的一级来源**。正式沉淀必须回到原始网页、论文、公告或项目仓库。
- 晋升判据同铁律 4：一次性动态留在 `briefs/`；同一论点出现连续证据、或单个奠基性来源足以形成稳定
  判断时，才建议晋升。晋升走完整 research 沉淀流程（见核心操作 1 第 5 步），**不得整份复制进 wiki**。
- 默认滚动保留最近 30 天。自动化不得自行删除超期文件；月末可提醒用户确认删除，或在用户明确要求
  长期保留时移入 `briefs/archive/YYYY-MM/`。删除后说明范围与可恢复性。

## output/ HTML 呈现层约定（派生视图层）

`output/` 是**给人读的 HTML 交付层**：把知识渲染成可直接打开、可直接发出去的单文件网页。
**它是交付物，不是知识资产**——不计入页面数、不进索引、**不进 lint**，你不为它的新鲜度负责。
它对 wiki 只有一条义务，方向朝内不朝外：遵循 [[代码与文档漂移的本质]] 的原则一，
**可以重复展示，不能让知识只住在这里**。

### 两类用途，两套约束

| | `output/personal/`（自用） | `output/shared/`（对外分享） |
|---|---|---|
| 读者 | 只有用户自己 | 同事、客户、公开链接 |
| 文件名 | `<slug>.html`，稳定名 | `<slug>-YYYY-MM-DD.html`，日期即版本 |
| 修订 | 直接覆盖重渲染 | **不得原地覆盖**：另存新日期文件，旧文件保留 |
| 内容 | 无限制：可含内部代号、未定稿判断、粗略数据 | 不得含内部代号、客户名、内部路径、未核实数据；不确定的判断标注置信度 |
| 引用 | 可保留 `[[双链]]` | 双链必须展开为标题或外部 URL（外部环境解析不了双链） |

冻结理由：文件一旦发出，链接与副本已在别人手里，原地改写会造成"同一文件名指向不同内容"——
这就是表达漂移的对外版本。

personal → shared 是一次**显式动作**，不是移个目录：过一遍脱敏与可溯源检查，再另存为带日期的新文件。
方向不可逆——内部件误发为对外件不可撤回，反之无损。因此**存量或来源不明的 HTML 一律按"已对外发出"
处理：不覆盖、不删除、不改写，先问用户**。

### 不得成为第二事实源

- **原创稿必须回灌 wiki。**（本节唯一的硬要求）若一份 HTML 的内容量超出现有 wiki 沉淀
  （典型：对外方案稿），那份判断事实上已成了权威产物却只住在交付物里。必须把可复用的**论点**
  回写成 wiki 页面，`output/` 只保留渲染结果。
  注意方向：这保护的是 **wiki 的完整性**（别让知识只存在于一份没人维护的交付物里），
  不是把 output 纳入知识管理。
- **不手改 HTML。** 要改内容 → 改对应 wiki 页 → 重新渲染。直接改 HTML 会让同一份内容出现两个版本。
  这是为了你手上不出现自相矛盾的两份文件，不是对 output 的维护义务。
- **建议（不强制、不校验）**：页内写清来源与生成日期——机器可读的
  `<meta name="canonical-source">` / `<meta name="generated">`，页脚给人读的一行来源 + 生成日期 +
  外部引用。纯粹方便日后自己回溯，缺了不算问题，lint 不检查。
- **不写入 `wiki/index.md`，不计入 wiki 页面数**，不建索引，**wiki 页也不登记 `renders:`**。
  `renders:` 字段自 2026-08-31 起废止：不再新增、不再校验、不再据其触发重渲染；
  存量字段留着无害，下次编辑该页时顺手删掉即可。

### 技术约定（单文件自包含）

- 单个 `.html`：**inline CSS/JS，零 CDN，零构建**，图片走 data URI——断网可读、可直接当附件发出。
- `<html lang="zh-CN">` + `<title>` + viewport meta；宽内容（表格、代码块）自带横向滚动容器，
  页面本体不横向滚动。
- 视觉沿用现有 editorial 风格（ivory/slate/clay/olive/rust + ui-serif/system-ui），由 `thariq-html`
  skill 承载；换风格先说明理由，避免风格漂移。
- 交付前自查：本地打开无 console 报错、无外部网络请求、移动端宽度不溢出。

### 日志与删除

- **`shared/` 的生成与更新必记 `wiki/log.md`**（前缀 `output`），因为已对外发出、需要可追溯；
  `personal/` 不记（可随时重生成，无对外后果）。
- **删除**：本仓库是 git 仓库（主分支 `main`），已提交内容可恢复；未提交改动与 `.gitignore` 内
  文件不可恢复。`output/` 下任何删除仍必须先问用户。

## 第三方工具兼容

本仓库布局已适配 [nashsu/llm_wiki](https://github.com/nashsu/llm_wiki) 桌面应用，使其可直接作为
project 打开（图谱可视化、社区检测、hybrid 检索）。注意：**Karpathy 原文没有规定目录结构**，
这套布局是那个实现的约定，不是"标准"——适配它买的是生态兼容性。约束：

- **Claude Code 是 `wiki/` 的唯一写手**（铁律 7）。该 App 只当阅读器与图谱/检索工具，
  **不得用它的 ingest 写 wiki**：它会用自己的 LLM 重写页面、丢弃它不认识的 frontmatter 字段，
  `overview.md` 会被整篇覆写，且它按「每源必建 source 页」工作，与铁律 4 相反。
- 若开启它的 source 文件夹自动监听：必须把 `output`、`briefs`、`.claude`、`.agents` 加入 excludeDirs，
  并从 includeExtensions 去掉 `html`/`htm`——否则 `output/shared/` 的对外 HTML 会被当素材摄入。
- `.llm-wiki/`（它的本机项目状态与 UUID）已在 `.gitignore` 中，不入库。
- 它从**项目根**读 `purpose.md`/`schema.md`，另一条摄入路径从 `wiki/` 读——故根目录保留两个 symlink
  指向 `wiki/` 下的真身。
- 它对未知目录的处理是「目录名即页面类型」，所以 `analyses/`、`topics/` 无需改名即可被识别。

**通用 `llm-wiki` skill 的路径覆盖**：本机安装的 `llm-wiki` skill（`.claude/skills/llm-wiki/`、
`.agents/skills/llm-wiki/`）其 `SKILL.md` 仍按「`index.md` / `log.md` 在仓库根」的默认布局描述流程
（约 11 处）。**本文件覆盖它**：在本仓库内一律读写 `wiki/index.md` 与 `wiki/log.md`，绝不在根目录
重建这两个文件。不修改已安装 skill 的文件（它是通用件，且会被升级覆盖）。
